CREATE TABLE IF NOT EXISTS "author" (
    id         INTEGER PRIMARY KEY,
    name       VARCHAR(255)
);

CREATE TABLE IF NOT EXISTS "book" (
    id         INTEGER PRIMARY KEY,
    title      VARCHAR(255)
);
